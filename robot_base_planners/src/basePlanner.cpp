#include "robot_planners/basePlanner.h"

/**

  Base Class Planner implementation

**/


void BasePlanner::drawDirection(const arma::vec3 &direction, const arma::vec3 &position){
    /*glPushMatrix();
    glTranslatef(position(0),position(1),position(2));
    GLTools::DrawOutline(true);
    glScaled(0.2,0.2,0.2);
    GLTools::SetColor(1,1,0,1);
    GLTools::DrawVector(Vector3(direction(0),direction(1),direction(2)));
    glPopMatrix();*/
}

